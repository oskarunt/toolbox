{
  "name": "toolbox",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "generate": "node scripts/generate-readme.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test"
  }
}
