{
  "name": "toolbox",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "generate": "node scripts/generate-readme.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
